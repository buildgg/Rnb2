import { RnbCellDirective } from './rnb-cell.directive';

describe('RnbCellDirective', () => {
  it('should create an instance', () => {
    const directive = new RnbCellDirective();
    expect(directive).toBeTruthy();
  });
});
